 - name: Basic server setup
   hosts: localhost  # Assuming Jenkins is running on the same machine
   become: true       # Use sudo for elevated privileges
   tasks:
     - name: Example task
       debug:
         msg: "Hello, this is a basic setup"
